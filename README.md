# Rage of Titans Token

## Install dependencies

`npm install`

## Run Development (Start up the remixd server)

```
  Usage: npm run dev

  Starts the remixd server in project's root directory. After that, you can go to https://remix.ethereum.org and connect to localhost under the Workspaces.

```